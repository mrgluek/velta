import java.util.Properties

plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("rust")
}

val tauriProperties = Properties().apply {
    val propFile = file("tauri.properties")
    if (propFile.exists()) {
        propFile.inputStream().use { load(it) }
    }
}

android {
    compileSdk = 36
    namespace = "org.velta"
    defaultConfig {
        manifestPlaceholders["usesCleartextTraffic"] = "false"
        applicationId = "org.velta"
        minSdk = 24
        targetSdk = 36
        versionCode = tauriProperties.getProperty("tauri.android.versionCode", "1").toInt()
        versionName = tauriProperties.getProperty("tauri.android.versionName", "1.0")
    }
    buildTypes {
        getByName("debug") {
            manifestPlaceholders["usesCleartextTraffic"] = "true"
            isDebuggable = true
            isJniDebuggable = true
            isMinifyEnabled = false
            packaging {                jniLibs.keepDebugSymbols.add("*/arm64-v8a/*.so")
                jniLibs.keepDebugSymbols.add("*/armeabi-v7a/*.so")
                jniLibs.keepDebugSymbols.add("*/x86/*.so")
                jniLibs.keepDebugSymbols.add("*/x86_64/*.so")
            }
        }
        getByName("release") {
            // Minification is disabled for this proof-of-concept because the
            // Tauri/Rust JNI bridge and plugin commands are easily broken by
            // ProGuard stripping. The APK is larger but startup is reliable.
            isMinifyEnabled = false
            isShrinkResources = false
        }
    }
    // (Kotlin jvmTarget moved to the top-level `kotlin { compilerOptions }`
    // block below — the old `kotlinOptions { jvmTarget }` DSL is a hard
    // error under KGP 2.3. Re-apply after regenerating.)
    buildFeatures {
        buildConfig = true
    }
}

rust {
    rootDirRel = "../../../"
}

// KGP 2.3 migration of the old `kotlinOptions { jvmTarget = "1.8" }` block.
// This file is autogenerated — re-apply after `cargo tauri android init`.
kotlin {
    compilerOptions {
        jvmTarget.set(org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_1_8)
    }
}

dependencies {
    implementation("androidx.webkit:webkit:1.14.0")
    // Chrome Custom Tabs for the in-app message-link browser (InAppBrowser.kt).
    implementation("androidx.browser:browser:1.8.0")
    implementation("androidx.appcompat:appcompat:1.7.1")
    implementation("androidx.activity:activity-ktx:1.10.1")
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.lifecycle:lifecycle-process:2.10.0")
    // UnifiedPush connector: registration + push-event plumbing with the
    // user-selected distributor app (no Play Services involved).
    implementation("org.unifiedpush.android:connector:3.3.4-rc1")
    testImplementation("junit:junit:4.13.2")
    androidTestImplementation("androidx.test.ext:junit:1.1.4")
    androidTestImplementation("androidx.test.espresso:espresso-core:3.5.0")
}

apply(from = "tauri.build.gradle.kts")